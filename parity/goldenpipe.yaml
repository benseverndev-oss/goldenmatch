# Cross-language API parity manifest for goldenpipe (MCP tools + CLI commands).
# See docs/superpowers/specs/2026-07-04-api-parity-gate-design.md.
# GENERATED by `check_api_parity.py goldenpipe --init`, human-reviewed (2026-07-05). The CI
# api_parity gate fails if the real Python/TS surface stops matching this partition; every
# tool/command add/remove/rename must be reflected here in the same PR.
# Review: MCP tools at FULL parity. CLI: only `interactive` is Python-only. No naming divergence.
package: goldenpipe
mcp_tools:
  shared:
  - explain_pipeline
  - list_stages
  - run_pipeline
  - validate_pipeline
  python_only: []
  ts_only: []
cli_commands:
  shared:
  - agent-serve
  - init
  - mcp-serve
  - run
  - serve
  - stages
  - validate
  python_only:
  - interactive
  ts_only: []
