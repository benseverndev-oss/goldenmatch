# Cross-language API parity manifest for goldencheck (MCP tools + CLI commands).
# See docs/superpowers/specs/2026-07-04-api-parity-gate-design.md.
# GENERATED by `check_api_parity.py goldencheck --init`, human-reviewed (2026-07-05). The CI
# api_parity gate fails if the real Python/TS surface stops matching this partition; every
# tool/command add/remove/rename must be reflected here in the same PR.
# Review: MCP tools near-parity (only install_domain is Python-only — INTENTIONAL: the TS
#   core exposes a read-only domain registry (listAvailableDomains / getDomainTypes /
#   DOMAIN_REGISTRY, src/core/index.ts) with no install path, so there is no TS operation
#   to reconcile). CLI: Python still ships more commands, but they are servers/daemons/
#   interactive (agent-serve/serve/schedule/scan-db/init/review) or unported engines
#   (denial-constraints/refs/learn). `history` + `evaluate` are now SHARED — the TS port
#   already carries the engines (core/engine/history.ts + evaluateScan), so the CLI
#   commands were wired over them. `health-score` / `list-domains` / `profile` are now
#   SHARED too — the Python CLI wraps the existing scan/profile/domain engines (the same
#   ones the MCP `health_score` / `profile` / `list_domains` tools use). No naming divergence.
# a2a_skills: FULL A2A parity — Python and TS expose the same 9 AgentCard skill ids
#   (analyze_data, compare_domains, configure, explain, fix, handoff, review, scan, validate).
#   No divergence.
package: goldencheck
mcp_tools:
  shared:
  - analyze_data
  - approve_reject
  - auto_configure
  - compare_domains
  - explain_column
  - explain_finding
  - get_column_detail
  - get_domain_info
  - health_score
  - list_checks
  - list_domains
  - pipeline_handoff
  - profile
  - review_queue
  - review_stats
  - scan
  - suggest_fix
  - validate
  python_only:
  - install_domain
  ts_only: []
cli_commands:
  shared:
  - agent-serve
  - baseline
  - demo
  - diff
  - evaluate
  - fix
  - health-score
  - history
  - list-domains
  - mcp-serve
  - profile
  - scan
  - validate
  - watch
  python_only:
  - denial-constraints
  - init
  - learn
  - refs
  - review
  - scan-db
  - schedule
  - serve
  ts_only: []
a2a_skills:
  shared:
  - analyze_data
  - compare_domains
  - configure
  - explain
  - fix
  - handoff
  - review
  - scan
  - validate
  python_only: []
  ts_only: []
