# Cross-language API parity manifest for goldenmatch (MCP tools + CLI commands).
# See docs/superpowers/specs/2026-07-04-api-parity-gate-design.md.
# GENERATED by `check_api_parity.py goldenmatch --init`, then human-reviewed (2026-07-05).
# Each surface is partitioned into shared / python_only / ts_only; the CI `api_parity`
# gate fails if the real Python/TS surface stops matching this partition. Every add/
# remove/rename of a tool or command must be reflected here in the same PR.
#
# Review of the current gaps (three categories):
#   1. NAMING ALIASES (resolved 2026-07-05 via non-breaking aliases — both
#      servers now answer to both names, so these are shared: dedupe/find_duplicates,
#      match/match_record, explain_pair/explain_match, profile/profile_data, and
#      explain_cluster (alias of the shared agent_explain_cluster)). CLI info + score
#      are now SHARED — the Python CLI wraps score_strings + the config allow-lists.
#      tui(TS) vs interactive(PY) are NOT aliases (distinct ops) — left as a coverage gap.
#   2. PYTHON-RICHER COVERAGE (intentional — Python is the fuller, STATEFUL server):
#      get_stats / get_cluster / list_clusters are Python-only BECAUSE the TS MCP is
#      stateless — its dedupe() returns stats + clusters inline (DedupeResult), so there
#      is no server-held dataset to query. Domains (create_domain/list_domains/test_domain)
#      and PPRL (pprl_link/pprl_auto_config) are Python-only subsystems. Plus identity-audit
#      trio, routing tools, learning-memory plugins, and the Python-only CLI commands
#      (autoconfig, anomalies, lineage, sensitivity, serve-ui, ...).
#   3. TS-SPECIFIC PRIMITIVES (intentional — edge helpers): score_strings, score_pair,
#      find_exact_matches, find_fuzzy_matches, read_file, write_csv, server_info.
#
# NOTE: goldencheck's sole py-only MCP tool `install_domain` is likewise intentional
# (TS core exposes a read-only domain registry, no install path). That annotation
# lands in parity/goldencheck.yaml once PR #1449 (the 6-package rollout) merges.
#
# a2a_skills surface (A2A AgentCard skills). The core ops share canonical ids and ARE shared
# (deduplicate, match, explain, evaluate, autoconfig, compare_strategies, transform, identity_*,
# learn_thresholds, memory_stats, ...). RECONCILED 2026-07-05: autoconfig/auto_configure,
# compare_strategies/agent_compare_strategies, transform/run_transforms were verified same-op
# and aliased to Python's canonical id (TS card advertises the canonical, legacy tool id still
# dispatches) — now shared.
# RECONCILED 2026-07-26 (a2a-fracture): the #1114 MDM read-side trio identity_profile /
# identity_stats / identity_worklist were ts_only on the A2A card yet already SHARED on the MCP
# surface (Python has entity_profile/identity_summary_stats/steward_worklist + the MCP tools) —
# a surface inconsistency, not a real divergence. Python's A2A card now advertises them
# (a2a/server.py _SKILLS + skills.py delegates to mcp.identity_tools._dispatch), moving all three
# ts_only -> shared.
# Remaining a2a_skills divergences are VERIFIED-DIFFERENT ops, NOT drift: PY `quality` is one
# scan+fix skill vs TS's `scan_quality` + `fix_quality` (1:2 granularity); PY `pprl` RUNS
# linkage (`pprl_link`, two files) while TS `suggest_pprl` SUGGESTS params (`profileForAgent`,
# "recommend") — TS suggest_pprl is the A2A analogue of Python's own suggest_pprl, not pprl.
# TS also exposes the agent_* skill variants
# (agent_deduplicate/agent_match_sources/agent_explain_pair/agent_explain_cluster/
# agent_review_queue/agent_approve_reject) and edge primitives (score, memory_import); Python
# ships the richer identity-audit trio + routing/lifecycle skills. The
# TS set is the deduped union of BASE_SKILLS + AGENT_SKILLS + MEMORY_TOOLS + IDENTITY_TOOLS
# (src/node/a2a/server.ts). The registry-introspection trio
# (list_scorers/list_transforms/list_strategies) is now SHARED — Python exposes them on both
# the MCP tool surface and the A2A card (stateless serializers over the config allow-lists).
package: goldenmatch
mcp_tools:
  shared:
  - add_correction
  - agent_approve_reject
  - agent_compare_strategies
  - agent_deduplicate
  - agent_explain_cluster
  - agent_explain_pair
  - agent_match_sources
  - agent_review_queue
  - analyze_blocking
  - analyze_data
  - auto_configure
  - build_clusters
  - certify_recall
  - certify_semantic_model
  - certify_serving_joins
  - compare_clusters
  - config_weaknesses
  - controller_telemetry
  - convert_splink_config
  - create_domain
  - customer_360
  - dedupe
  - emit_semantic_model_from_store
  - evaluate
  - explain_cluster
  - explain_match
  - explain_pair
  - export_results
  - find_duplicates
  - find_exact_matches
  - find_fuzzy_matches
  - fix_quality
  - get_cluster
  - get_golden_record
  - get_stats
  - identity_audit
  - identity_audit_seal
  - identity_audit_verify
  - identity_claim
  - identity_conflicts
  - identity_history
  - identity_list
  - identity_merge
  - identity_profile
  - identity_resolve
  - identity_resolve_conflict
  - identity_show
  - identity_split
  - identity_stats
  - identity_worklist
  - incremental
  - learn_thresholds
  - lineage
  - list_blocking_strategies
  - list_clusters
  - list_corrections
  - list_domains
  - list_runs
  - list_scorers
  - list_strategies
  - list_transforms
  - match
  - match_record
  - memory_export
  - memory_import
  - memory_stats
  - pprl_link
  - profile
  - profile_data
  - read_file
  - retrieve_similar
  - review_config
  - rollback
  - run_transforms
  - scan_quality
  - schema_match
  - score_pair
  - score_strings
  - sensitivity
  - server_info
  - shatter_cluster
  - suggest_config
  - suggest_pprl
  - test_domain
  - unmerge_record
  - upload_dataset
  - write_csv
  python_only:
  - discover_semantic_model
  - documents_ingest
  - documents_suggest_schema
  - explain_routing
  - lint_routing
  - list_plugins
  - ontology_certify
  - ontology_discover
  - plan_routing
  - pprl_auto_config
  ts_only: []
cli_commands:
  shared:
  - agent-serve
  - analyze-blocking
  - anomalies
  - autoconfig
  - certify-keys
  - compare-clusters
  - config
  - dedupe
  - demo
  - evaluate
  - explain
  - identity
  - import-splink
  - incremental
  - info
  - init
  - interactive
  - label
  - lineage
  - match
  - mcp-serve
  - memory
  - pprl
  - profile
  - review
  - rollback
  - runs
  - schedule
  - score
  - sensitivity
  - serve
  - tui
  - unmerge
  python_only:
  - discover-model
  - import-dbt
  - ingest-docs
  - llm
  - migrate-splink
  - ontology
  - serve-ui
  - setup
  - sync
  - watch
  ts_only: []
a2a_skills:
  shared:
  - add_correction
  - analyze_data
  - autoconfig
  - compare_strategies
  - controller_telemetry
  - deduplicate
  - evaluate
  - explain
  - identity_audit
  - identity_audit_seal
  - identity_audit_verify
  - identity_claim
  - identity_conflicts
  - identity_history
  - identity_list
  - identity_merge
  - identity_profile
  - identity_resolve
  - identity_resolve_conflict
  - identity_show
  - identity_split
  - identity_stats
  - identity_worklist
  - learn_thresholds
  - list_corrections
  - list_scorers
  - list_strategies
  - list_transforms
  - match
  - memory_export
  - memory_stats
  - profile
  - review_config
  - suggest_config
  - suggest_pprl
  - transform
  python_only:
  - analyze_blocking
  - certify_semantic_model
  - compare_clusters
  - configure
  - documents_ingest
  - documents_suggest_schema
  - incremental
  - list_runs
  - pprl
  - quality
  - retrieve_similar
  - review
  - rollback
  - schema_match
  - sensitivity
  ts_only:

  - agent_approve_reject
  - agent_deduplicate
  - agent_explain_cluster
  - agent_explain_pair
  - agent_match_sources
  - agent_review_queue
  - certify_serving_joins
  - customer_360
  - emit_semantic_model_from_store
  - fix_quality
  - memory_import
  - scan_quality
  - score
# (given_name_aliased_jw / name_freq_weighted_jw) are now FIRST-CLASS in Python
# FULL parity as of the name-scorer promotion: the census/alias name scorers
# VALID_SCORERS (were plugin-registry-only), matching the TS VALID_SCORERS — so
# a config referencing them validates + scores without a manual refdata import.
# both move ts_only -> shared. refdata registers them at `import goldenmatch`, so
# scorers surface (config.schemas.VALID_SCORERS <-> types.ts VALID_SCORERS).
scorers:
  shared:
  - alias_match
  - array_intersect
  - audio_fp
  - cosine
  - date
  - date_diff
  - dice
  - embedding
  - ensemble
  - exact
  - geo_haversine
  - given_name_aliased_jw
  - initialism_match
  - jaccard
  - jaro_winkler
  - levenshtein
  - name_freq_weighted_jw
  - numeric_diff
  - phash
  - qgram
  - radial
  - record_embedding
  - soundex_match
  - token_sort
  # FS domain comparators (specs 2026-07-23-fs-domain-comparators + 2026-07-25-
  # splink-domain-comparator-conversion): date_diff, geo_haversine, array_intersect,
  # numeric_diff (Part D), and now `cosine` (vector cosine over two precomputed
  # float-vector columns, score-core score_one id 23 + TS/WASM port) are ALL shared.
  # Every string/numeric scorer is kernel-single-sourced; python_only is empty.
  python_only: []
  ts_only: []
# transforms surface (config.schemas.VALID_SIMPLE_TRANSFORMS <-> types.ts
# VALID_TRANSFORMS). Currently in exact parity -- no declared deltas.
transforms:
  shared:
  - alpha_only
  - digits_only
  - first_token
  - last_token
  - lowercase
  - metaphone
  - normalize_whitespace
  - soundex
  - strip
  - strip_all
  - strip_honorifics
  - token_sort
  - uppercase
  python_only: []
  ts_only: []
# blocking_strategies surface (config.schemas BlockingConfig.strategy <->
# config-edits.ts VALID_BLOCKING_STRATEGIES). lsh (MinHash-LSH, sketch-core) +
# perceptual (banded-hamming pHash-LSH, perceptual-core) ported to TS 2026-07-25
# (buildLshBlocks/buildPerceptualBlocks, cross-language fixture
# blocking-strategies.json) -> shared. simhash stays python_only (see the
# blocking_kernels_deferred reason: model-backed, not edge-portable).
# token (DF-pruned inverted-index token blocking, #2488) is python_only FOR NOW:
# it is pure string/dict work with no kernel and no model, so it is portable in
# principle -- the deferral is sequencing, not a barrier. It lands first on the
# Python analyzer path it was written for (auto-suggest blocking selection);
# porting it to TS means a buildTokenBlocks + a cross-language fixture in
# blocking-strategies.json, and is tracked to follow once the analyzer side has
# settled its defaults (max_df derivation in particular, which the fixture would
# otherwise pin prematurely).
blocking_strategies:
  shared:
  - adaptive
  - ann
  - ann_pairs
  - canopy
  - learned
  - lsh
  - multi_pass
  - perceptual
  - sorted_neighborhood
  - static
  python_only:
  - simhash
  - token
  ts_only: []
# scorer_kernels surface: the scorers with a Rust/-core kernel (the REFERENCE fast
# path) -- Python `_NATIVE_SCORER_IDS` (backends.score_buckets) vs TS
# `WASM_COVERED_SCORERS` (core/wasm/backend SCORER_ID). Every scorer in the
# `scorers` surface NOT listed here is a pure-language fallback with no kernel.
# shared: `qgram` (id 5), `date` (id 4), `soundex_match` (id 6), `dice` (id 9),
# `jaccard` (id 10) are kernel-backed on BOTH surfaces -- Python arrow-native (bucket)
# AND TS WASM (score-wasm delegates the id to score_one; the WASM output is byte-exact
# with the pure-TS fallback -- qgram is rational set-Jaccard, date's true-DL==OSA for the
# equal-length 8-digit inputs it buckets, soundex_match is the shared Unicode-folding
# standard-Soundex spec + empty-guard, dice/jaccard are integer bloom-popcount + one divide).
# `ensemble` (id 12) is also shared: score-wasm overrides id 12 to recompose it with
# the TS-parity normalized token_sort (jw + normalized token_sort + 0.8*soundex_match),
# matching the pure-TS `ensembleScore` to 4dp -- the same bar its jw/token_sort
# components hold vs rapidfuzz (soundex byte-exact). `phash` (id 11) is also shared:
# perceptual-hash similarity (1 - hamming/nbits over hex pHashes), integer XOR popcount,
# byte-exact WASM<->pure-TS like dice/jaccard.
# `alias_match` (id 8) is now shared: score-wasm ENABLES the `alias` cargo feature (the
# `regex` engine, ~+36 KB) and the kernel needs TWO tables injected at enableWasm() --
# the business alias table (set_business_aliases: the FULL 81-entry legal-form variant
# list, from which the kernel rebuilds the trailing-suffix strip regex, + the surface->
# canonical alias map) and the given-name nickname map (set_given_name_canonicals). The
# loader seeds both with the ported refdata/business.ts + refdata/givenNames.ts tables so
# WASM == pure-TS byte-exact. `initialism_match` is shared: the kernel needs the ~77-entry
# legal_forms table injected at enableWasm() (set_legal_forms), which the loader seeds with
# the ported refdata/business.ts table so WASM == pure-TS byte-exact.
# `given_name_aliased_jw` / `name_freq_weighted_jw` are now shared too: the census/alias
# name scorers are kernel-backed on BOTH surfaces -- TS WASM (score-wasm ids 20/21 over
# fs-core) AND Python native (WEIGHTED bucket ids 15/16 in score_block_pairs/_arrow,
# dispatched through fs-core's `name_freq_weighted_sim`/`given_name_aliased_sim` over the
# process-global census/alias tables from `set_name_reference_data`, gated on the
# `NATIVE_SUPPORTS_NAME_BUCKET_SCORERS` capability + `_ensure_name_tables_installed`). ONE
# fs-core reference on both sides, so Python native == TS WASM by construction. Parity vs
# the pure-Python plugin is TOLERANCE-bounded (native base JW is rapidfuzz-rs vs the
# plugin's rapidfuzz-py; empirically machine-epsilon since there is no FS-style level
# banding) -- native is the reference. `name_freq_weighted_jw` declines native for a field
# carrying a per-dataset `tf_freqs` table (#1207): fs-core ports only the static-census
# branch, so those fields stay on the vectorized find_fuzzy_matches (like `date` declining
# on a stale wheel -- the kernel exists + is reachable, which is the shared contract).
scorer_kernels:
  shared:
  - alias_match
  - array_intersect
  - audio_fp
  - cosine
  - date
  - date_diff
  - dice
  - ensemble
  - exact
  - geo_haversine
  - given_name_aliased_jw
  - initialism_match
  - jaccard
  - jaro_winkler
  - levenshtein
  - name_freq_weighted_jw
  - numeric_diff
  - phash
  - qgram
  - radial
  - soundex_match
  - token_sort
  python_only: []
  # The FS domain comparators are ALL kernel-backed on BOTH Python-native
  # (score-core kernels + pyo3 shims) AND TS/WASM now -> shared: date_diff/
  # geo_haversine (score_one ids 17/18), array_intersect (id 19, jaccard default),
  # numeric_diff (id 22, pct:0.1 default). Their MODE/BAND-parameterized forms
  # (array_intersect:overlap, numeric_diff:abs|pct:<band>) ride the scorer string
  # which the fixed-id score_one can't carry, so those forms flow through the
  # spec-carrying kernel on the per-pair path (numeric_diff) / decline to pure
  # (array_intersect:overlap) -- but each scorer's DEFAULT is kernel-backed +
  # reachable, which is the `shared` contract (the `date`-declines-on-stale-wheel
  # rule). Parity: tests/parity/scorer-domain-comparators.test.ts + wasm-scorer.test.ts
  # + tests/test_native_{date_diff_geo,array_intersect,numeric_diff}_parity.py.
  ts_only: []
# scorer_kernels_deferred: the COVERAGE FLOOR. Every scorer in the `scorers` surface
# that is NOT kernel-backed (absent from `scorer_kernels`) MUST appear here with a
# reason -- the `check_scorer_coverage` gate FAILS on any uncovered scorer, so a new
# scorer, or a kernel that regresses to a pure-language fallback, can no longer sit
# unaddressed the way "5 of 19 kernel-backed" did (nothing forced per-scorer
# classification). Deferral is a conscious act with a rationale: `deferred --` = will
# kernelize (with the blocker); `declined --` = won't (with why); `n/a --` = not a
# string kernel. Removing a scorer's kernel without adding it here fails CI.
scorer_kernels_deferred:
  embedding: "n/a -- model-backed (sentence-transformers / Vertex); belongs to goldenembed, not a string kernel"
  record_embedding: "n/a -- model-backed multi-field embedding"
  # numeric_diff was kernelized 2026-07-25 (Part D) and cosine 2026-07-26 (score_one
  # id 23 + TS/WASM port) -> both moved to scorer_kernels. Every string/numeric
  # scorer is now kernel-backed; only the two model-backed embedding scorers remain.
# blocking_kernels_deferred: the COVERAGE FLOOR for the `blocking_strategies` surface
# (check_blocking_coverage). Every strategy NOT in `blocking_strategies.shared` (i.e.
# python_only / ts_only) MUST appear here with a reason, so a non-shared blocking
# strategy is a conscious, documented decision -- not a silent gap. (Unlike
# scorer_kernels this is a lighter manifest-map floor, not an emitted partition:
# blocking is mostly group-by orchestration, and only lsh/simhash/perceptual have a
# `-core` kernel, so there is no source-level "kernel set" to emit + compare.)
# `declined --` = won't port (with why); `deferred --` = will (with the blocker).
blocking_kernels_deferred:
  simhash: "declined -- model-backed: SimHash blocking embeds every record via a model (numpy + get_embedder); TS ships only the embedding kernel (goldenembed-wasm), no bundled edge model -- the same boundary that makes retrieve_similar require a caller-supplied embedder. Dense-vector blocking is already served by the shared ann/ann_pairs strategies. sketch-core provides the SimHash signature kernel, but the strategy is not edge-portable. (lsh + perceptual, the other two kernel-backed blockers, ARE ported -> shared.)"
  token: "deferred -- portable, not yet ported (#2488). DF-pruned inverted-index token blocking is pure string/dict work: tokenize (sketch._word_tokens, already ported as part of the shared lsh word-shingle path), count document frequency, keep tokens with 2 <= df <= max_df, emit one block per token. No kernel, no model, no numpy -- nothing about it is edge-hostile, which is why this is deferred rather than declined. The blocker is that the TS port needs a cross-language fixture in blocking-strategies.json, and pinning one now would freeze the max_df derivation (max_df_ratio * n clamped to [10, 1000]) before the Python analyzer side has settled it -- the auto-suggest integration is what tunes those defaults, and re-cutting the fixture on every tweak is the cost this avoids. Port once the defaults stop moving."
# ─────────────────────────────────────────────────────────────────────────────
# ADVISORY (non-gating) SQL surfaces — VISIBILITY ONLY. Added 2026-07-25.
# These record the goldenmatch SQL function inventory so the parity gate can
# REPORT (never fail on) whether the Postgres/DuckDB surfaces track the Python
# reference. They are intentionally NOT gating: the SQL surfaces delegate to the
# Python library for scoring (`goldenmatch_score(a,b,scorer)` -> the same
# `score_strings` / `score-core` reference), so scorers/transforms can't drift;
# operation coverage is a roadmap concern, not a per-PR gate.
#
# `postgres`  = goldenmatch-owned `#[pg_extern]` fns in the goldenmatch_pg pgrx
#               crate (siblings goldencheck_*/goldenflow_* excluded).
# `duckdb`    = goldenmatch-owned UDFs/macros the goldenmatch_duckdb package
#               registers (create_function + CREATE MACRO; siblings/`_impl` excluded).
#
# Both are emitted by STATIC source parse (scripts/emit_sql_surface.py — no pgrx
# toolchain, no built extension, box-safe) and reconciled by
# check_api_parity.check_sql_advisory(): DRIFT (source vs this inventory) is
# reported robustly; COVERAGE vs the Python MCP+CLI surface is a labeled
# heuristic. To refresh after adding/removing a SQL fn:
#   python3 scripts/emit_sql_surface.py goldenmatch   # then sync the lists below
# ─────────────────────────────────────────────────────────────────────────────
postgres:
  functions:
  - apply
  - correction_add
  - correction_list
  - gm_clusters
  - gm_configure
  - gm_drop
  - gm_embed
  - gm_golden
  - gm_identity_audit
  - gm_identity_audit_seal
  - gm_identity_audit_verify
  - gm_identity_claim
  - gm_identity_merge
  - gm_identity_profile
  - gm_identity_resolve_conflict
  - gm_identity_split
  - gm_identity_stats
  - gm_identity_worklist
  - gm_jobs
  - gm_pairs
  - gm_resolve
  - gm_run
  - gm_telemetry
  - goldenmatch_autoconfig
  - goldenmatch_autoconfig_telemetry
  - goldenmatch_autofix_table
  - goldenmatch_certify_structural
  - goldenmatch_compare_clusters
  - goldenmatch_connected_components
  - goldenmatch_connected_components_str
  - goldenmatch_dedupe
  - goldenmatch_dedupe_clusters
  - goldenmatch_dedupe_full
  - goldenmatch_dedupe_full_telemetry
  - goldenmatch_dedupe_pairs
  - goldenmatch_dedupe_table
  - goldenmatch_detect_anomalies
  - goldenmatch_detect_domain
  - goldenmatch_embed_local
  - goldenmatch_evaluate
  - goldenmatch_explain
  - goldenmatch_extract_features
  - goldenmatch_hnsw_pairs
  - goldenmatch_identity_conflicts
  - goldenmatch_identity_history
  - goldenmatch_identity_list
  - goldenmatch_identity_resolve
  - goldenmatch_identity_view
  - goldenmatch_lsh_pairs
  - goldenmatch_match
  - goldenmatch_match_pairs
  - goldenmatch_match_tables
  - goldenmatch_pair_dedup
  - goldenmatch_pair_dedup_str
  - goldenmatch_perceptual_hamming
  - goldenmatch_perceptual_phash
  - goldenmatch_postflight
  - goldenmatch_preflight
  - goldenmatch_profile_table
  - goldenmatch_record_fingerprint
  - goldenmatch_score
  - goldenmatch_score_pair
  - goldenmatch_score_probabilistic
  - goldenmatch_suggest_threshold
  - goldenmatch_train_em
  - goldenmatch_train_em_from_counts
  - goldenmatch_validate_table
  - memory_learn
  - memory_stats
duckdb:
  functions:
  - gm_configure
  - gm_drop
  - gm_golden
  - gm_jobs
  - gm_run
  - gm_telemetry
  - goldenmatch_autoconfig
  - goldenmatch_autoconfig_telemetry
  - goldenmatch_autofix_table
  - goldenmatch_certify_structural
  - goldenmatch_compare_clusters
  - goldenmatch_connected_components
  - goldenmatch_connected_components_str
  - goldenmatch_correction_add
  - goldenmatch_correction_list
  - goldenmatch_dedupe
  - goldenmatch_dedupe_full
  - goldenmatch_dedupe_table
  - goldenmatch_detect_anomalies
  - goldenmatch_detect_domain
  - goldenmatch_embed_local
  - goldenmatch_evaluate
  - goldenmatch_explain
  - goldenmatch_extract_features
  - goldenmatch_hnsw_pairs
  - goldenmatch_identity_conflicts
  - goldenmatch_identity_history
  - goldenmatch_identity_list
  - goldenmatch_identity_resolve
  - goldenmatch_identity_view
  - goldenmatch_lsh_pairs
  - goldenmatch_match
  - goldenmatch_match_tables
  - goldenmatch_memory_learn
  - goldenmatch_memory_stats
  - goldenmatch_pair_dedup
  - goldenmatch_pair_dedup_str
  - goldenmatch_perceptual_hamming
  - goldenmatch_perceptual_phash
  - goldenmatch_postflight
  - goldenmatch_preflight
  - goldenmatch_profile_table
  - goldenmatch_record_fingerprint
  - goldenmatch_score
  - goldenmatch_score_pair
  - goldenmatch_score_probabilistic
  - goldenmatch_suggest_threshold
  - goldenmatch_train_em
  - goldenmatch_validate_table
