# Context Network — Discovery Hub

The navigation entry point. Each node below is focused and cross-linked; follow the
links rather than reading everything.

## Foundation (what this is)
- [foundation/project-definition.md](foundation/project-definition.md) — what the Golden Suite / goldenmatch is, and the gate that governs the current arc.
- [foundation/structure.md](foundation/structure.md) — the polyglot monorepo layout and where the authoritative context lives (CLAUDE.md files).

## Architecture (active technical knowledge)
- [architecture/datafusion-spine.md](architecture/datafusion-spine.md) — the embedded-DataFusion "scale mode" spine (Stages A-E); current status + entry points.
- [architecture/sail-tier.md](architecture/sail-tier.md) — the distributed Sail-native tier (Spark Connect) that replaces Ray; specced, build not started.
- [architecture/distributed-wcc.md](architecture/distributed-wcc.md) — the Ray distributed-WCC solve for #844: randomized-contraction connected components + the recall-complete Phase-5 path (block-shuffle scoring + distributed clustering); specs SHIPPED (#851/#852) + #864 finish-line fixes; VALIDATED at 100M (9.2 min e2e, 20M clusters exact); default flipped ON (#867).
- [architecture/sql-native-extensions.md](architecture/sql-native-extensions.md) — graph + embedding UDFs on DuckDB/Postgres/DataFusion, native-direct (shared `graph-core` + `goldenembed-rs`); SHIPPED (#509).
- [architecture/goldenflow-native-kernel.md](architecture/goldenflow-native-kernel.md) — GoldenFlow date/phone vectorized fast paths + the optional `goldenflow-native` phone kernel (NANP-only gated); SHIPPED (2026-06-07).
- [architecture/goldencheck-native-kernel.md](architecture/goldencheck-native-kernel.md) — GoldenCheck's Arrow-native runtime (`goldencheck-native`) + deep-profiling expansion (Benford / composite-key / FD / fuzzy / approx-FD kernels, `--deep`, `refs`, freshness) + the `cell_quality` / `functional_dependencies` bridge APIs; SHIPPED (#793, 2026-06-07).
- [architecture/goldencheck-goldenmatch-integration.md](architecture/goldencheck-goldenmatch-integration.md) — data quality feeds entity resolution: four fail-open, default-OFF doors (survivorship, blocking, FD negative-evidence, quality-gated review); #794/#795/#798 shipped, #797 open.
- [architecture/fellegi-sunter-splink-parity.md](architecture/fellegi-sunter-splink-parity.md) — the `type: probabilistic` matchkey from scorer to Splink-class engine: model lifecycle, supervised m, match-weight waterfall, calibration, accuracy analysis, bucket/native scale-out. FS auto-config v2 (#823, v1.29.0) now beats hand-rolled Splink on every dataset Splink scores (pairwise F1, shared evaluator), made reproducible by the #829 EM-sampling determinism fix; the deterministic three-engine bake-off is at [`docs/benchmarks/2026-06-09-splink-bakeoff.md`](../docs/benchmarks/2026-06-09-splink-bakeoff.md).
- [architecture/rust-test-coverage.md](architecture/rust-test-coverage.md) — how the `packages/rust/extensions/` crates are tested in CI: per-crate map, the bridge CPython-in-CI dance, the `cargo pgrx test` structural dead-end, and the measured per-crate baseline; SHIPPED (#827/#830/#832, 2026-06-09).
- [architecture/wasm-acceleration.md](architecture/wasm-acceleration.md) — opt-in WASM acceleration for the pyo3-free `*-core` crates → TypeScript: the shared `goldenmatch-wasm-runtime`, `score-core`→goldenmatch (jaro_winkler/levenshtein/token_sort/exact) + `analysis-core`→goldenanalysis (histogram/quantile), parity+bench gates, the rapidfuzz-alignment prerequisite; SHIPPED #878/#879/#880/#881; graph/fingerprint/goldencheck parked.
- [architecture/single-kernel-collapse-R1-plan.md](architecture/single-kernel-collapse-R1-plan.md) — the single-kernel-collapse R1 go/no-go plan: Workstream A (WASM across Node/browser/Workers/Deno = kill-criterion 2) + Workstream B (all-platform abi3 wheels + the #688 perf-cliff = kill-criterion 3, PROBED by `r1-kernel-wheels.yml`), the "one generalized gate run in more places" + `kernel-targets` report idea, the additive/no-default-flip-in-R1 rule, and the honest expected-PARTIAL outcome. See also the [inventory](architecture/single-kernel-collapse-inventory.md) + [roadmap](architecture/single-kernel-collapse-roadmap.md) + [ADR 0016](decisions/0016-single-kernel-collapse-spike.md).

## Decisions (records with no other home)
- [decisions/0001-gate-reframe-engine-portability.md](decisions/0001-gate-reframe-engine-portability.md) — retire one-box RSS as the gate; engine portability is the destination.
- [decisions/0002-scale-mode-contract.md](decisions/0002-scale-mode-contract.md) — `mode={standard,scale}`, opt-in, semantically-correct-not-bit-identical.
- [decisions/0003-stage-e-spill-honest-null.md](decisions/0003-stage-e-spill-honest-null.md) — one-box spill-survival does not bind (the UF island); default stays opt-in.
- [decisions/0004-sail-tier-scope.md](decisions/0004-sail-tier-scope.md) — Sail tier: full, buildable, Sail-native, replaces Ray; WCC-on-Sail is the gate.
- [decisions/0005-sql-native-direct-udfs.md](decisions/0005-sql-native-direct-udfs.md) — SQL graph + embed UDFs go native-direct (drop the CPython bridge); shared `graph-core`, accept-both ids, embed wheel, 3 surfaces.
- [decisions/0006-goldenflow-native-nanp-gating.md](decisions/0006-goldenflow-native-nanp-gating.md) — GoldenFlow: vectorize in Polars first; gate the native phone kernel to NANP-only (parity-safe by construction).
- [decisions/0007-goldencheck-goldenmatch-integration.md](decisions/0007-goldencheck-goldenmatch-integration.md) — GoldenCheck→GoldenMatch: fail-open quality bridges, additive, default-OFF + benchmark-gated; hold the DQ↔ER boundary.
- [decisions/0008-fellegi-sunter-splink-parity.md](decisions/0008-fellegi-sunter-splink-parity.md) — Fellegi-Sunter: close the Splink engine gap in dependency order, reuse the scale substrate, keep defaults reproducible (new power opt-in), measure the scale gate on a real runner.
- [decisions/0009-rust-test-coverage.md](decisions/0009-rust-test-coverage.md) — Rust coverage: make the tests real (de-skip the bridge, run the standalone crates), route around the `cargo pgrx test` dead-end (psql smoke), then measure — informational baseline, not a hard gate.
- [decisions/0010-publish-containers-ghcr-mirror.md](decisions/0010-publish-containers-ghcr-mirror.md) — publish-containers flakes were anonymous-Docker-Hub buildkit-pull timeouts; mirror buildkit/binfmt into ghcr (off the hot path) + native retry-once, no new secrets/actions.
- [decisions/0011-distributed-wcc-randomized-contraction.md](decisions/0011-distributed-wcc-randomized-contraction.md) — distributed WCC: randomized contraction (relational, chain-robust) over driver-collect / min-propagation; per-round parquet checkpoint dodges the Ray streaming-executor deadlock; VALIDATED at 100M (9.2 min) + default-flipped (#867); the e2e wall was per-group scoring (vectorized in #864), not the WCC.
- [decisions/0012-fs-block-scoring-perf.md](decisions/0012-fs-block-scoring-perf.md) — FS block-scoring perf: "Splink 3-19x faster" was a numpy-path red herring (the bake-off never set `GOLDENMATCH_FS_NATIVE`); the wall is per-block fan-out, not scoring math, so the Rust kernel can't move it; three output-identical numpy optimizations took historical_50k −72% local.
- [decisions/0013-goldencheck-ts-parity-hardening.md](decisions/0013-goldencheck-ts-parity-hardening.md) — goldencheck TS port: finish module parity (2 profilers / 4 relations / `validate`, mirroring the Python fallback) + harden the golden harness (assert confidence + affected_rows, fail-on-missing, regenerate on a CI runner since the box OOMs on Polars); it caught a pre-existing `temporal_order` over-fire on integer columns (#855 closed).
- [decisions/0014-opt-in-wasm-acceleration.md](decisions/0014-opt-in-wasm-acceleration.md) — opt-in WASM for the TS packages: pure-TS stays the default + fallback, one shared runtime (extracted not duplicated), batch-first per-scorer swap, ship a core only on a measured parity+bench win (parked graph-core on the verdict, not a crate); the bench-as-dist-validation gate caught a real 1M-element histogram overflow.
- [decisions/0015-goldenmatch-2.0-deprecation-cut.md](decisions/0015-goldenmatch-2.0-deprecation-cut.md) — GoldenMatch 2.0: cut exactly the four prepared deprecation-window items (`:hash:` bridge + `GOLDENMATCH_IDENTITY_ID_SCHEME`, `GOLDENMATCH_CLUSTER_FRAMES_OUT` gate + legacy dict cluster path, two dead shims); the scorer `list[tuple]` pair path stays as the permanent universal representation; `build_clusters` stays public as a frames-backed adapter; un-fingerprintable rows keep `:hash:`; the sharded-CI-vs-targeted-local lesson (stale `mock.patch` on a removed import).
- [decisions/0018-collective-er-neighborhood-similarity.md](decisions/0018-collective-er-neighborhood-similarity.md) — collective ER: `run_graph_er(propagation_mode="relational")` blends attribute + neighbor-cluster similarity (Bhattacharya-Getoor) and iterates to a Jacobi fixpoint; measured F1 ~0.66→~0.87 where attributes can't resolve homonyms; flat-boost `additive` shown harmful (~0.05); P2 negative-evidence / P3 learned-weights deferred. SHIPPED #1030 (Phase 0+1).
- [decisions/0016-single-kernel-collapse-spike.md](decisions/0016-single-kernel-collapse-spike.md) — single-kernel-collapse feasibility: a fully-additive spike (R0 inventory + a generalizable `pure==kernel` 4dp equivalence gate on the levenshtein tracer + a measure-first bench) with a verbatim 4-item kill criterion; in-env the tracer cleared on BOTH Python (native abi3, bit-identical) and TS (WASM, 4dp) bindings and the kernel was 1.44x faster, but cross-JS-target WASM (Node-only verified) and all-platform abi3 wheels (the #688 class) stay PENDING — template proven, platform-reliability gates not. See also [architecture/single-kernel-collapse-inventory.md](architecture/single-kernel-collapse-inventory.md) + [architecture/single-kernel-collapse-roadmap.md](architecture/single-kernel-collapse-roadmap.md).
- [decisions/0017-dbt-parity-program.md](decisions/0017-dbt-parity-program.md) — dbt-goldensuite parity (P0–P4): relocate to `packages/dbt` + restore the CI lane; plan/execute `autoconfig(table, mode)` for zero-config Fellegi-Sunter; pipeline UDFs (dedupe/match/autoconfig) stay JSON-over-the-bridge (Arrow shelved, see [0001](decisions/0001-gate-reframe-engine-portability.md)) vs the native-direct kernels ([0005](decisions/0005-sql-native-direct-udfs.md)); pure-SQL `goldenmatch_match_quality` test; table-returning `goldenmatch_match_pairs` (Postgres-first) with `reference_id` normalized to 0-based; GoldenAnalysis stays Python-side. P5 (transform breadth) deferred.
- [decisions/0019-immutable-releases-publish-flow.md](decisions/0019-immutable-releases-publish-flow.md) — publish-goldenmatch.yml now owns the GitHub Release: push a `v*` tag → build → PyPI → cosign sign+attest → **draft** release with assets attached → publish (the only sequence that attaches signed bundles under GitHub's immutable-releases setting, which broke the old attach-after-publish flow on v2.1.0). New SOP: cut a release by pushing the tag only. SHIPPED #1063.
- [decisions/0020-minhash-lsh-sketch-tier.md](decisions/0020-minhash-lsh-sketch-tier.md) — MinHash/LSH sketch tier (#1081, phase 1 of the training-data dedup epic #1080): new pyo3-free `sketch-core` crate (shingling → MinHash → banded LSH) on Python (native + fallback) + TS, a `MinHashLSHBlocker` / `strategy="lsh"`, hand-rolled hash for byte-parity (golden vectors), Approach A (kernel sketches, host groups), `sketch` native-available-but-not-gated (like `pprl_bloom`), measured recall via a synthetic gate + a Quora-QQP bench job.
- [decisions/0021-goldenmatch-kg-integrations.md](decisions/0021-goldenmatch-kg-integrations.md) — `goldenmatch-kg`: drop goldenmatch in as the entity-resolution stage of neo4j-graphrag (real `GoldenMatchResolver` plugin over `BasePropertySimilarityResolver.run`), LlamaIndex PGI (additive name-canonicalizing `TransformComponent`), and Graphiti (post-ingestion `propose_entity_merges` pass — no public seam). Standalone package EXCLUDED from the uv workspace (keeps the heavy framework extras out of the main lock); framework-agnostic `resolve_entities` core; lift read off ER-KG-Bench (neo4j 0.322→0.969, graphiti 0.379→0.969); shim tests stub the goldenmatch decision because a toy zero-config merge is version-flaky. SHIPPED #1127.
- [decisions/0022-corpus-dedup-throughput-benchmark.md](decisions/0022-corpus-dedup-throughput-benchmark.md) — corpus-dedup throughput benchmark + per-PR perf gate (#1086, epic #1080): a `bench_corpus_dedup/` harness (pluggable FineWeb/C4/Wikipedia/offline adapters + injected ground-truth near-dups so recall is measurable) and a DETERMINISTIC `throughput-gate` (machine-independent cost vs a committed baseline, can't flake on shared runners). Published number: ~1,192 docs/sec at 70k FineWeb, ~0.43 measured LSH recall (auto-config-bound wall; raw sketch ≈7,800/s). The bench proved the #1083 claim was overstated at scale — surfaced+fixed 4 at-scale bugs (O(N²) quality scan, web-text mis-classification, ≥100k RED refusal, open: O(N) survivorship iter_rows ceiling). SHIPPED #1134/#1139/#1142/#1144/#1147.
- [decisions/0024-autoconfig-probabilistic-routing.md](decisions/0024-autoconfig-probabilistic-routing.md) — the decision-kernel quality harness (`scripts/autoconfig_quality/`, report/gate/bless; 2-tier host-independent signals + F1-attribution; real benchmarks + synthetic anchors; CI `quality_gate`) and the first lever it nominated on evidence: **probabilistic routing**. Auto-config routes a dataset to Fellegi-Sunter (`GOLDENMATCH_AUTOCONFIG_ROUTE_PROBABILISTIC=1`, default-off) iff no surviving exact matchkey is backed by a strong-identity col (`{identifier,email,phone}` — widened from identifier-only because clean-email exact beats FS) + ≥2 fuzzy fields. Dual-strategy column proved historical_50k 0.466→0.829, zero regression. Default-flip deferred. SHIPPED #1216/#1226; routing #1254.
- [decisions/0025-healer-verify-gate-proxy.md](decisions/0025-healer-verify-gate-proxy.md) — healer self-verify gate default proxy flipped to cohesion (closes the raw-vs-live suggestion gap; bake-off-selected).
- [decisions/0026-healer-default-pipeline.md](decisions/0026-healer-default-pipeline.md) — healer wired into the default `dedupe_df` pipeline as a two-stage cost-bounded advisory surface (free controller trigger → cheap artifacts-in attach; `suggest=`/`heal=` opt-in for the expensive paths) on every surface; graceful-degrade without native; kill-switch `GOLDENMATCH_SUGGEST_ON_DEDUPE=0`.
- [decisions/0027-healer-wasm-ts.md](decisions/0027-healer-wasm-ts.md) — the config-suggestion **healer** on the TS/WASM surface: compile the existing pyo3-free `suggest-core` kernel to a `suggest-wasm` cdylib (mirroring the autoconfig `-core → -wasm → TS` precedent), feature-gate arrow + add an arrow-free `suggest_from_json` shared with the Python path (zero Python change), wire it into `dedupe({suggest, heal})` at full default-pipeline parity on every TS surface (core/CLI/MCP `review_config` → 45 tools/A2A), opt-in `enableSuggestWasm()` (the `[native]` analog) with graceful-empty default, one cross-surface golden-vector contract (TS == Rust == Python). Kill-switch `GOLDENMATCH_SUGGEST_ON_DEDUPE`.
- [decisions/0031-goldenflow-reference-mode-identifiers-wasm.md](decisions/0031-goldenflow-reference-mode-identifiers-wasm.md) — GoldenFlow Wave 0: move to the suite-standard `-core`/`-native`/`-wasm` split (new pyo3-free `goldenflow-core` OWNS the phone + identifier kernels; `native-flow` is a thin shim; new `goldenflow-wasm` edge binding), adopt the reference-mode `_has_symbol`/`_FALLBACK_ONLY` loader (retire the `_GATED_ON` allowlist under `auto`), add 10 checksummed-identifier transforms (cc/iban/isbn/ean/vat — registry 76→86, all `auto_apply=False`), and route them cross-surface via opt-in `enableWasm()` with one shared byte-parity corpus (goldenflow-core = oracle) + a `wasm_flow` CI lane. goldenflow 1.4.0 / npm 0.4.0 / goldenflow-native 0.2.0.
- [decisions/0032-goldenflow-duckdb-compiled-extension.md](decisions/0032-goldenflow-duckdb-compiled-extension.md) — GoldenFlow's fourth conforming surface: `goldenflow-duckdb`, a compiled zero-Python DuckDB loadable extension linking `goldenflow-core` directly (74 UDFs `goldenflow_<kernel>`, parity via the same shared corpus, portable to DuckDB >= 1.3.0, 5-platform zip distribution). Also closes the lockstep seam — every goldenflow-core consumer (`rust`, `python_goldenflow_fallback`, `wasm_flow`, `rust_pgrx`, `goldenflow_duckdb`, `native_flow`) is now a required CI gate. `goldenflow-duckdb-v0.1.1`.
- [decisions/0034-goldenflow-fused-columnar-apply.md](decisions/0034-goldenflow-fused-columnar-apply.md) — GoldenFlow Pillar-1 (evict Polars from execution): fuse a column's chain of owned no-arg total string→string kernels (25: text/email/name) into ONE native Arrow pass (`chain::apply_chain`, generic over offset since Polars exports LargeUtf8), byte-identical output + audit. Default-ON (opt-out `GOLDENFLOW_FUSED_APPLY=0`), needs `goldenflow-native >= 0.12.0`. MEASURED: wall 1.07–1.27× (config-dependent) but the durable win is **peak RSS −22% at scale** — an RSS play, not a speed play. `goldenflow-native 0.12.0` / `goldenflow 1.14.0`.
- [decisions/0035-denial-constraint-discovery.md](decisions/0035-denial-constraint-discovery.md) — GoldenCheck Stage 1: a new opt-in discovered-rule family mining denial constraints `¬(p1 ∧ … ∧ pm)` (if-then / cross-tuple invariants) from a single table. Sample-then-validate, two evidence passes (row-level exact + pairwise sampled), order-preserving RANK encoding (not `intern_column`), native `dc.rs` measure-first (~1.5–1.8× over a Polars cross-join, set/byte-parity), findings as `check="denial_constraint"`. Opt-in via `--denial` / the `denial-constraints` CLI / `discover_denial_constraints`; NOT in the default scan. Stage 1 of a 5-stage program.
- [decisions/0036-goldenmatch-polars-optional-arrow-native.md](decisions/0036-goldenmatch-polars-optional-arrow-native.md) — GoldenMatch 3.1.0: polars becomes an optional `[polars]` extra; the engine is Arrow-native end to end (spine descent D2s + lane widening W-1..7 + arrow ports A1-A9, PRs #1720-#1736). Polars-present paths are wall optimizations, byte-identical to 3.0.x; a zero-polars CI gate + a bridge-count tripwire are the invariants. K-series kernels resolved as a measured no-go (precompute = 13.9% of a 1M wall).
- [decisions/0037-goldenflow-owned-auto-detect-profile-kernel.md](decisions/0037-goldenflow-owned-auto-detect-profile-kernel.md) — GoldenFlow owned auto-detect profile kernel: the zero-config type-inference *decision* (`infer_type` on every surface + fused `profile_column` on the columnar path) is now owned by `goldenflow-core`, cross-surface byte-identical via `tests/parity/profile_corpus.jsonl`, native-first (opt-out `GOLDENFLOW_NATIVE=0`). A distinct owned SURFACE, not a registered transform (so out of the `test_owned_kernel_boundary.py` buckets). Accepted known edge: pure-TS strip-then-slice vs Python/Rust slice-then-strip on the ≤100 sample. `goldenflow-native 0.27.0` / `goldenflow 2.1.0`.
- [decisions/0038-fs-negative-evidence-em-learned.md](decisions/0038-fs-negative-evidence-em-learned.md) — NE on Fellegi-Sunter matchkeys is EM-learned (Formulation B): `__ne__<field>` constrained 2-state EM dimensions with a storage-only `[w_fired, 0]` clamp (no labels needed — the Wave D "needs labels" deferral was stale), `penalty_bits` fixed override; loud declines (continuous both surfaces, TS pipeline, fused `derive_from`); converted Splink configs get the `fan_out` upgrade lever (risk-gated NE suggestion + `max_cluster_size` guard tuning); native + TS score NE behind capability gates (`FS_SUPPORTS_NE`); parity pinned bit-exact. `goldenmatch 3.3.0` / native 0.1.15 / js 1.3.0.
- [decisions/0040-embeddings-first-class-fellegi-sunter.md](decisions/0040-embeddings-first-class-fellegi-sunter.md) -- Embeddings first-class on Fellegi-Sunter: `embedding` / `record_embedding` train (EM) and score end-to-end via the vectorized matrix (they CRASHED before — `score_field` has no embedding branch). Vectorized EM E-step (`_build_comparison_matrix` + `_embedding_pair_sims` + `field_sims` on `comparison_vector`, train<->score level parity), un-gate + `record_embedding` scoring branch, model-backed forces vectorized regardless of `GOLDENMATCH_FS_VECTORIZED=0`, TUI routes FS through `probabilistic_block_scorer`. NE model-backed still scalar (deferred). Fake-embedder tests + opt-in real-embedder e2e. PR #1806 (FS-audit batch with #1800/#1801).
- [decisions/0039-precision-anchor-commit-demotion.md](decisions/0039-precision-anchor-commit-demotion.md) -- Precision-anchor threshold raise with commit-time demotion: the new default rule `rule_precision_anchor_threshold_raise` raises the weighted threshold to 0.9 on the precision-collapse shape (mass_above_threshold >= 0.95 + name-only weighted matchkey + strong exact identity anchor + live TF table + threshold < 0.9), its trigger (`precision_anchor_would_fire`) doubles as a labels-free precision-collapse detector at commit time (`pick_committed` `demote_suspect`, rank +1, default byte-identical), and the dip statistic needs minimum support (`_MIN_DIP_SUPPORT = 30`). Either commit-dynamics fix alone still commits the over-merge -- probe-validated. Ride-along: the `n_rows` shadow that silently disabled the `REFUSE_AT_N` refuse gate is fixed. Crafted-fixture precision 0.009 -> 0.9868 at recall 1.0; NCVR unaffected. goldenmatch main (unreleased, PR #1786).
- [decisions/0033-goldenanalysis-cutover-waves-and-parity-fixtures.md](decisions/0033-goldenanalysis-cutover-waves-and-parity-fixtures.md) — GoldenAnalysis cutover: the **cut-vs-fixture rule** — cut to `analysis-core` only for MUSCLE with a clean `Float64Array`-shaped boundary (numeric reductions mean/min/max, cluster_size_histogram — all three surfaces), otherwise enforce Python↔TS parity with a data-driven fixture (frame-kernel equality semantics, quality.rollup, regressions — trivial-compute and/or object-shaped, no clean Rust boundary). The frame-kernel adversarial fixture caught + fixed a real TS `duplicateRowRatio` NaN/null-conflation bug. Wave 1b (WASM for the frame kernels) consciously deferred (no clean boundary, speculative browser payoff).
- [decisions/0041-fs-missing-value-semantics.md](decisions/0041-fs-missing-value-semantics.md) — FS missing-value semantics: a missing comparison is `unobserved` (no evidence) by default, but the library does NOT impose one meaning — `MatchkeyConfig.missing ∈ {unobserved,disagree}` + `GOLDENMATCH_FS_MISSING`, and auto-config's `_pick_missing_semantics` picks `disagree` at ≥20% max field null-rate (historical_50k 0.83→0.33 under a blanket `unobserved`, hence per-dataset). Native declines `disagree` (kernel is neutral-only) → numpy. Ride-alongs: weighted renormalize by observed weight (#1856), the `weights[-1]` maximal-evidence calibration bug (#1861), the #1835→#1836 blocking-field fixed-prior repair. goldenmatch 3.4.0 (#1834/#1851/#1872).
- [decisions/0042-native-kernel-owns-fs-coverage.md](decisions/0042-native-kernel-owns-fs-coverage.md) — Native kernel owns 100% FS scorer coverage: the FS math extracts to a pyo3-free `fs-core` crate (consumed by native + `fs-wasm`, parity by construction), the kernel gains ids for the last fall-back scorers (name_freq_weighted_jw / given_name_aliased_jw / tf_adjustment / ensemble / embedding), and numpy is retained as the reference-mode lossy fallback + parity oracle (deleting it is deferred; base install without `[native]` still runs FS). Wheel-skew `FS_SUPPORTS_*` capability consts; native 0.1.16/0.1.18. goldenmatch 3.4.0 (#1869/#1871/#1808/#1876).
- [decisions/0043-bucket-default-fs-route.md](decisions/0043-bucket-default-fs-route.md) — Bucket is the default FS route (one shared `_fs_use_bucket_route` gate, native-optional, no row cap, data-scaled n_buckets), and FS now scores in the distributed / chunked / strategy lanes (one shared `EMResult` per matchkey). Enabled by #1808 (Arc exclude handle + zero-copy Arrow) and block-size safety (#1784 scale-proportional `max_safe_block`, #1790/#1829 auto-split + dense-matrix guard, #1857 null-key filter). person-1M: `MemoryError` → 139s/1.72GB. goldenmatch 3.4.0 (#1810/#1843/#1844).
- [decisions/0044-learned-blocking-compiler-field-transforms.md](decisions/0044-learned-blocking-compiler-field-transforms.md) — Learned blocking lowers to `multi_pass` via per-field transform chains (`BlockingKeyConfig.field_transforms`, chosen over lossy pass-splitting; `from_splink` maps mixed SUBSTR exactly). A refuse-rather-than-approximate compiler + a which-records differential harness; the drop-cap-must-not-sit-below-selection-budget invariant (#1838). STATUS: compiler landed but INERT (wiring #1845 closed invalid). goldenmatch 3.4.0 (#1831/#1841/#1840).
- [decisions/0045-quality-gate-required-watches-own-surfaces.md](decisions/0045-quality-gate-required-watches-own-surfaces.md) — quality_gate must watch every surface that moves its numbers (`filters.yml` + a `check_filter_coverage.py` self-test) and must block merges (`ci-required.needs`). Root cause: historical_50k f1_probabilistic 0.83→0.33 merged red on `main` because the gate skipped `probabilistic.py` and was non-blocking. Fixes the visibility, not the #1846 regression itself. goldenmatch 3.4.0 (#1847/#1877).
- [decisions/0046-cross-language-phase-handoff-conformance.md](decisions/0046-cross-language-phase-handoff-conformance.md) — Cross-language phase-handoff (Python↔TS) is governed by a measured conformance harness, not assumed from surface parity. Published per-boundary limits: identity graph + `score→cluster` + end-to-end split-run are byte-safe (identity also cryptographically cross-verifies); string scoring is 4dp tolerance-bounded (can flip a threshold); standardize/dates + embeddings + controller diverge; distributed/VLM/routing are Python-only. Harness = Python oracle → TS parity test (`cluster-conformance` + `split-run`). Docs `concepts/cross-language-parity`, design note 2026-07-24. 2026-07-24.

## Processes (how work is done here)
- [processes/development-workflow.md](processes/development-workflow.md) — spec → plan → execute → review → CI → merge, plus the hard environment constraints.

## Planning (where it's going)
- [planning/roadmap.md](planning/roadmap.md) — the Arrow-native arc and what's next.
- [planning/surface-hardening.md](planning/surface-hardening.md) — the 2026-06-05 four-surface audit arc: fail-closed HTTP auth, CLI/TUI fixes, Python->TS parity ports, and the parity-fixture methodology — now including the #856/#857 "fixtures rot silently" lesson, the merged #857 refdata-name-scorer + autoconfig-blocking TS port, and the merged #855 goldencheck TS port (2 profilers / 4 relations / `validate` + a hardened golden harness that caught a pre-existing temporal_order bug).
- [planning/discoverability.md](planning/discoverability.md) — the 2026-06-13 public-surface audit arc: accuracy-first, value-forward README + `llms.txt` family (now complete: all packages + root + the new goldenanalysis/infermap), PyPI/npm keyword refresh + `CITATION.cff`, the GitHub About, and the archived-repo redirects. The "verify every public number" rule, the README-callout-from-CHANGELOG single source of truth, the Mintlify-auto-serves-`/llms.txt` fact, and the archived-repo-is-API-read-only gotcha. Open: social-preview image + external awesome-list PRs (PR #883).
- [planning/security-hardening.md](planning/security-hardening.md) — the 2026-06-05 security-hardening arc: 42-alert remediation (Dependabot + code scanning), Scorecard 6.1->7.3 (Token-Permissions/Signed-Releases/Fuzzing), the CodeQL Autofix incident, property-test bug ledger, and open actions.

## Reference (operational)
- **Runtime config / opt-ins:** the single source of truth for every `GOLDENMATCH_*` environment variable — native gate, backend selection, distributed pipeline, perf opt-ins — is the published Mintlify page `docs-site/goldenmatch/tuning.mdx` (`docs.bensevern.dev/goldenmatch/tuning`). Built 2026-06-13 after a day lost to silent pure-Python fallback + `backend="ray"`-doesn't-distribute confusion. Defaults verified against source; keep it in sync when adding an env read. Don't re-derive the flag list from memory — read that page (or re-grep `GOLDENMATCH_` read-sites) instead.

## Meta (keeping the network alive)
- [meta/updates.md](meta/updates.md) — chronological change log for the network.
- [meta/maintenance.md](meta/maintenance.md) — how to keep nodes accurate and small.

---
**Classification:** navigation • **Last updated:** 2026-06-13
