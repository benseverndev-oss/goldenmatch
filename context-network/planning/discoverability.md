# Discoverability — the 2026-06-13 public-surface audit arc

An accuracy-first, value-forward sweep of every surface a human or an AI crawler
hits before installing: the root README, the `llms.txt` family, the GitHub repo
About, package-registry metadata, and the external discovery channels. Shipped on
PR #883 (docs-only) plus live GitHub-settings edits. Sibling to the
[surface-hardening](surface-hardening.md) arc (which is the CLI/TUI/HTTP/API
*functional* surfaces — distinct from these *discovery* surfaces).

## The governing rule
**Lead value-forward, but every public number is verified against source before it
ships.** The audit pass for each surface was: (1) diff what the repo actually offers
against what the surface claims, (2) fix every inaccuracy, (3) restructure to surface
the under-sold high-value work. Numbers are checked against the bake-off doc,
`Cargo.toml` deps, `__all__`, MCP `Tool(` / A2A `_SKILLS` counts, registry metadata —
never asserted from memory.

## README (root) — accuracy + positioning
- **Corrections:** DBLP-ACM `97.2% → 96.4%` (matches the committed `0.9641`);
  dropped the `DQBench ER 95.30` headline badge (that is the *competitor ceiling*,
  not a GoldenMatch score — DQbench `91.04` stays in the benchmarks section only,
  do not reintroduce as a headline); TS test count `478 → ~940`; package-README size
  `910 → ~1,300` lines; `36+ MCP tools → 50+` (goldenmatch alone ships 54);
  Identity Graph relabelled "the Identity Graph v2 *feature*, shipped in GoldenMatch
  v1.15" (it is NOT a package `v2.0`; latest release is `1.30.0`).
- **Positioning:** lead the accuracy proof with the reproducible **beats hand-rolled
  Splink** head-to-head + bake-off badge; added the under-sold GoldenAnalysis (6th
  package), the opt-in WASM backend, and the durable Identity Graph.
- **Single source of truth for the homepage "what's new":** the
  `<!-- README-callouts:start … end -->` block is generated by
  `scripts/sync_readme_callouts.py` from the top-3 `<!-- README-callout -->` markers
  in `packages/python/goldenmatch/CHANGELOG.md` (newest first). Edit the CHANGELOG
  marker, never the README block; `--check` is a CI gate. The homepage now leads with
  the v1.30.0 beats-Splink callout.

## llms.txt — the AI-discovery family
- **Coverage is now complete:** every package has an `llms.txt` (goldenmatch,
  goldencheck, goldenflow, goldenpipe + the newly-added **goldenanalysis** and
  **infermap**), plus `packages/rust/extensions/llms.txt` and a new **suite-level
  root `/llms.txt`** (the llmstxt.org standard location, parallel to the root README).
- **The docs site is Mintlify** (`docs-site/docs.json`), which **auto-generates and
  serves `/llms.txt` + `/llms-full.txt`** at `docs.bensevern.dev` from the docs
  content. The repo-root `llms.txt` is the GitHub/raw supplement, not the canonical
  web-root file.
- **Audit corrections applied to goldenmatch's llms.txt (both the canonical and
  `docs/` copies):** tagline now leads with beats-Splink + 100M scale; new Accuracy
  section; "Performance Limits (~500K cap)" → "Performance & Scale" with the verified
  100M run; **A2A skill count `10`/`12` → `31`** (the agent card's `_SKILLS` grew via
  an MCP-parity pass — both copies were stale); added the TS/Edge/WASM + SQL surfaces.
- Left the Python export count (`~101`) and CLI command count alone — they tangle with
  the docs-site's own published numbers (`__all__` is 167, the API-ref page says 101);
  "fixing" them here would create a new inconsistency.

## Registry + citation metadata
- **PyPI keywords were behind npm** (the registry where "entity resolution python" /
  "splink alternative" searches happen). Refreshed all 6: goldenmatch gained
  `record-linkage`, `fuzzy-matching`, `splink`, `fellegi-sunter`, `pprl`,
  `jaro-winkler`, `llm`; infermap 4 → 10; goldencheck got competitor terms
  (`great-expectations`, `pandera`); `golden-suite` added to all six for
  cross-discovery. npm keywords aligned to match.
- **`CITATION.cff`** (new, root) — GitHub "Cite this repository" button + Zenodo-DOI
  support; ER / record linkage is an academically cited domain.
- All live PyPI `[project.urls]` and npm `homepage`/`repository` already point at the
  monorepo — the published-package discovery path was already clean.

## GitHub About (live setting)
- Description rewritten to lead with beats-Splink + the recall-complete 100M number
  (was stale on the `213s` per-partition path, which is now the non-default
  under-merges path); added the edge-TS/WASM + SQL-native breadth. 332/350 chars,
  ASCII.
- Topics: added `splink` (the whole pitch is "beats Splink"); dropped the
  internal-jargon `negative-evidence`. Still 20/20.

## Archived sibling repos — redirected
The pre-fold standalone repos (`goldencheck`, `goldenflow`, `goldenpipe`, `infermap`)
still exist under `benseverndev-oss` as **archived**. Two fixes:
- **In-repo:** swept every `llms.txt` so the bare repo-root + "Part of the Golden
  Suite" cross-links point at the live monorepo package paths
  (`…/goldenmatch/tree/main/packages/python/<pkg>`); `/wiki` links left (they still
  resolve on the archived repos, no monorepo equivalent).
- **On the archived repos:** their About now redirects to the monorepo. **Gotcha
  (durable):** an archived repo is fully API-read-only — `gh repo edit` returns
  `HTTP 403 "Repository was archived so is read-only."` even for the description, so
  the redirect required **unarchive → edit description+homepage → re-archive** per
  repo (done for all 4, verified back to `archived=true`).

## Open / handed off
- **GitHub social preview image** — no API to upload it; manual in Settings → General
  → Social preview (1280×640 PNG). The card that renders on shared links.
- **External awesome-list PRs (prepared, not yet opened — they touch third-party
  repos):** `punkpeye/awesome-mcp-servers` (canonical MCP list),
  `OlivierBinette/Awesome-Entity-Resolution` (authoritative ER list),
  `kwanUm/awesome-data-quality` (for GoldenCheck). Ready-to-submit entries drafted;
  pending a go-ahead to open them under `benzsevern`. MCP directories beyond Smithery
  (glama.ai / mcp.so / PulseMCP) largely auto-index from the official MCP Registry +
  GitHub, so the awesome-mcp-servers PR is the main manual lever.
- **`llms-full.txt`** still missing for goldenanalysis + infermap (low priority — the
  concise `llms.txt` is what crawlers key off); Mintlify nav is thin for
  goldenpipe/infermap (1 entry each).

## Sub-arc: POST-INSTALL agent discoverability (2026-08-08)

The 2026-06-13 pass above covers the surfaces a human or crawler hits **before**
installing. This is the complement: what an AI agent can reach **after** install,
working inside someone else's repo.

**The observed problem:** agents consuming GM packages elsewhere reverse-engineer
behaviour from source rather than reading GM's docs.

**The diagnosis was not "the docs are missing".** `llms.txt` is good. It was
*unreachable*: it sat BESIDE each package directory, so `packages = [...]` never
shipped it. An agent in a foreign repo sees only `site-packages/<pkg>/`, cannot
reach this repo, and finds no pointer in the installed artifact — reverse
engineering was the only path available, not the lazy one.

**Inventory: 36 distributable surfaces; 8 had an `llms.txt`; none shipped it.**
(11 PyPI wheels, 10 npm, 10 maturin wheels, 2 crates.io crates, 1 SQL extension
pair, 3 GitHub Actions, 1 dbt package.)

**The rule, per ecosystem** — "ships inside the artifact" differs by packaging:
- **PyPI / maturin** — the file must live INSIDE the importable package
  (`goldenmatch/llms.txt`). Do NOT `force-include` from the package root: that
  re-adds an already-collected path and triggers the "Duplicate filename in local
  headers" failure that broke the v1.7.0 publish.
- **npm** — package root is what ships; add to `files`.
- **crates.io** — `include = [...]` plus `//!` crate docs (what docs.rs renders).
- **SQL extensions** — no filesystem; a `goldenmatch_docs()` UDF is the surface.
- **Actions / dbt** — consumed by ref; `action.yml` description + repo root.

**Why in-package and not wheel-only:** a `force-include` ships correctly but makes
`Path(pkg.__file__).parent / "llms.txt"` FALSE in a source checkout and an editable
install — wrong for exactly the contributor-side agent it is meant to help. A
pointer that lies is worse than no pointer. Moving the file makes it true in all
three.

**Also shipped per package:** `Documentation` → `docs.bensevern.dev/<pkg>` plus an
`AI agents (llms.txt)` URL (PyPI renders and exposes these), and an
authoritative-sources block opening the `__init__` docstring that says WHY —
behaviour here is decided and contract-tested, so the implementation shows what one
path does but not which guarantees are load-bearing.

**Gotcha for whoever extends this:** the per-package pointers in the ROOT
`llms.txt`, `docs/benchmark-claims.json`'s tracked-surface list, and
`scripts/check_llms_counts.py` all hardcode the per-package path. Moving the file
breaks all three, and each lives in a different file type — a `*.py`-only grep
misses the JSON, and a grep excluding `*.txt` misses the root index.

**Status:** Python wheels done (PR #2437). Remaining: npm ×10, maturin ×10, the
`goldenmatch_docs()` UDF, crates.io ×2, Actions ×3, dbt, error-message pointers,
and a discoverability gate extending `check_docs_consistency.py` (which already
builds the authoritative roster and already REPORTS `llms.txt` presence without
asserting it) with a per-ecosystem rule + deferral map.

---
**Classification:** planning/workstream • **Last updated:** 2026-08-08
